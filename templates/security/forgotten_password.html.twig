<!DOCTYPE HTML>
<html lang="en">
  <head>
    <meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	
    <link rel="canonical" href="https://getbootstrap.com/docs/4.0/examples/sign-in/">

	<!-- Bootstrap core CSS -->
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">

    <!-- Custom styles for this template -->
	<!--<link rel="stylesheet" href="signin.css">-->

	<style>
		html,
		body {
		height: 100%;
		}

		body {
		-ms-flex-align: center;
		align-items: center;
		padding-top: 40px;
		padding-bottom: 40px;
		background-color: #f5f5f5;
		}

		.form-signin {
		width: 100%;
		max-width: 330px;
		padding: 15px;
		margin: auto;
		}
		.form-signin .checkbox {
		font-weight: 400;
		}
		.form-signin .form-control {
		position: relative;
		box-sizing: border-box;
		height: auto;
		padding: 10px;
		font-size: 16px;
		}
		.form-signin .form-control:focus {
		z-index: 2;
		}
		.form-signin input[type="email"] {
		margin-bottom: -1px;
		border-bottom-right-radius: 0;
		border-bottom-left-radius: 0;
		}
		.form-signin input[type="password"] {
		margin-bottom: 10px;
		border-top-left-radius: 0;
		border-top-right-radius: 0;
		}
		.btn-perso {
			background: red;
			color: white;
		}
		.tiny-font {
			font-size: 0.7em;
		}
	</style>

  </head>
  <body>
	<div class="container text-center">
		<img class="mb-4" src="ressource\logo_Crous.png" alt="" width="120" height="120">
		<h1 class="h3 mb-3 font-weight-normal"><strong>CROUS Clermont-Auvergne</strong></h1>
		<h5 class="h3 mb-3 font-weight-normal">Entrez votre adresse mail pour recevoir une demande de reset de mot de passe</h5>

		<form class="form-signin" action="{{ path('security.forgotten_password') }}" method="POST">
			{% for message in app.flashes('message') %}
			<div class="alert alert-success" role="alert">
				{{ message }}
			</div>
			{% endfor %}
			{% for message in app.flashes('warning') %}
			<div class="alert alert-danger" role="alert">
				{{ message }}
			</div>
			{% endfor %}

			<!-- <label for="email" class="sr-only">Entrez votre adresse mail pour recevoir une demande de reset de mot de passe</label> -->
			{{ form_start(form) }}
			{{ form_row(form.email) }}
			{{ form_end(form) }}
			<!--<input type="email" id="inputEmail" name="_username" class="form-control" placeholder="adresse email" required autofocus> -->

			<button class="btn btn-lg btn-perso btn-block" type="submit">
				Envoyer
			</button>
		</form>
		<div class="form-signin">
			<a href="{{ path('security.login') }}">Revenir à la connexion</a>
		</div>
	</div>
  </body>
</html>