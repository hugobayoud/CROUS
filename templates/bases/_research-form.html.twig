<div class="search-box">
	<p><strong>Rechercher un service</strong></p>
	{{ form_start(form) }}
		{{ form_widget(form.q) }}
		<button type="submit" class="btn btn-primary">Filtrer</button>
		<a href="?q=" class="btn bouton-crous">Reset</a>
	{{ form_end(form) }}
</div>